import 'package:flutter/material.dart';

class MusicPageTopBar extends StatelessWidget {
  const MusicPageTopBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("music top"),
    );
  }
}
