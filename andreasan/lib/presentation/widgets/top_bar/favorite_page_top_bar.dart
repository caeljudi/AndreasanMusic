import 'package:flutter/material.dart';

class FavoritePageTopBar extends StatelessWidget {
  const FavoritePageTopBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("favorite top"),
    );
  }
}
