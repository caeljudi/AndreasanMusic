import 'package:flutter/material.dart';

class ProfilePageTopBar extends StatelessWidget {
  const ProfilePageTopBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("profile top"),
    );
  }
}
 